-- This script lists all the record, containing only the score and name columns of the table, named second_table and ordered the record by score column
SELECT score, name FROM second_table ORDER BY score DESC;
